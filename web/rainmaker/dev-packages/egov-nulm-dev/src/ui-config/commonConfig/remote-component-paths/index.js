const remoteComponentPath = (moduleName, path) => {
  //alert('aaaaaaaaaa')
  
  let component = null;
  switch (moduleName) {
    default:
      break;
  }
  return component;
};

export default remoteComponentPath;
